server:
  port: 51601
spring:
  application:
    name: leadnews-app-gateway
  cloud:
    nacos:
      discovery:
        server-addr: hmtt:8848
      config:
        server-addr: hmtt:8848
        file-extension: yml
